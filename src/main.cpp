#include <iostream>
#include "../include/Game.h"
#include "../include/Blocks.h"

int main(void)
{
    std::srand(static_cast<unsigned>(time(NULL)));

    Blocks blocks;
    Game game;
    return 0;
}
